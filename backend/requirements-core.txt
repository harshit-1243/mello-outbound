# Core booking engine + REST API + dashboard backend (app.main).
# Installs and tests on SQLite with no external accounts, and is ALL the deployed web service
# needs — no heavy voice/ML dependencies. The voice agent adds more (see requirements.txt).
sqlalchemy>=2.0,<2.1
pydantic>=2.7
pydantic-settings>=2.3
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0

# Production database driver (Postgres / Supabase). Omit for SQLite-only local use.
psycopg[binary]>=3.2
